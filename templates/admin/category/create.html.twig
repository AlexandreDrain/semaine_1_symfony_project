{% extends 'admin/admin_base.html.twig' %}

{% block title %}Administration des articles/create{% endblock %}

{% block body %}
<h1 class="text-center mt-5 mb-5">Créer un article</h2>

    {{ form_start(categoryForm) }}
        {{ form_widget(categoryForm) }}
        <input type="submit" class="btn btn-outline-success">
    {{ form_end(categoryForm) }}

    <a href="{{ path('admin_article') }}" class="btn btn-outline-mer">Retour à la liste</a>

{% endblock %}